import { SyncValidation } from '@ioc:Adonis/Core/Validator';
/**
 * Ensure the value is a valid bigint. Numeric string will be casted
 * to valid bigint
 */
export declare const bigint: SyncValidation;
